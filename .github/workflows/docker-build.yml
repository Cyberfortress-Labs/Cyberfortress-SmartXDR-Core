# name: Build and Push Docker Images

# on:
#   push:
#     branches:
#       - main
#     tags:
#       - 'v*'
#     paths:
#       - 'Dockerfile'
#       - 'nginx/Dockerfile'
#       - 'requirements.txt'
#       - 'app/**'
#       - '.github/workflows/docker-build.yml'
#   pull_request:
#     branches:
#       - main
#     paths:
#       - 'Dockerfile'
#       - 'nginx/Dockerfile'
#       - 'requirements.txt'
#       - 'app/**'
#   workflow_dispatch:

# env:
#   REGISTRY: docker.io
#   CORE_IMAGE: wanthinnn/smartxdr-core
#   NGINX_IMAGE: wanthinnn/smartxdr-nginx

# jobs:
#   build-and-push-core:
#     runs-on: ubuntu-latest
#     permissions:
#       contents: read
#       packages: write

#     steps:
#       - name: Checkout repository
#         uses: actions/checkout@v4

#       - name: Set up Docker Buildx
#         uses: docker/setup-buildx-action@v3

#       - name: Log in to Docker Hub
#         if: github.event_name != 'pull_request'
#         uses: docker/login-action@v3
#         with:
#           registry: ${{ env.REGISTRY }}
#           username: ${{ secrets.DOCKERHUB_USERNAME }}
#           password: ${{ secrets.DOCKERHUB_TOKEN }}

#       - name: Extract metadata for Core (tags, labels)
#         id: meta-core
#         uses: docker/metadata-action@v5
#         with:
#           images: ${{ env.REGISTRY }}/${{ env.CORE_IMAGE }}
#           tags: |
#             type=ref,event=branch
#             type=ref,event=pr
#             type=semver,pattern={{version}}
#             type=semver,pattern={{major}}.{{minor}}
#             type=semver,pattern={{major}}
#             type=raw,value=latest,enable={{is_default_branch}}
#             type=sha,prefix={{branch}}-

#       - name: Build and push SmartXDR Core image
#         uses: docker/build-push-action@v5
#         with:
#           context: .
#           file: ./Dockerfile
#           push: ${{ github.event_name != 'pull_request' }}
#           tags: ${{ steps.meta-core.outputs.tags }}
#           labels: ${{ steps.meta-core.outputs.labels }}
#           cache-from: type=gha
#           cache-to: type=gha,mode=max
#           platforms: linux/amd64
#           build-args: |
#             BUILDKIT_INLINE_CACHE=1

#       - name: Core image digest
#         run: echo ${{ steps.build-and-push-core.outputs.digest }}

#   build-and-push-nginx:
#     runs-on: ubuntu-latest
#     permissions:
#       contents: read
#       packages: write

#     steps:
#       - name: Checkout repository
#         uses: actions/checkout@v4

#       - name: Set up Docker Buildx
#         uses: docker/setup-buildx-action@v3

#       - name: Log in to Docker Hub
#         if: github.event_name != 'pull_request'
#         uses: docker/login-action@v3
#         with:
#           registry: ${{ env.REGISTRY }}
#           username: ${{ secrets.DOCKERHUB_USERNAME }}
#           password: ${{ secrets.DOCKERHUB_TOKEN }}

#       - name: Extract metadata for Nginx (tags, labels)
#         id: meta-nginx
#         uses: docker/metadata-action@v5
#         with:
#           images: ${{ env.REGISTRY }}/${{ env.NGINX_IMAGE }}
#           tags: |
#             type=ref,event=branch
#             type=ref,event=pr
#             type=semver,pattern={{version}}
#             type=semver,pattern={{major}}.{{minor}}
#             type=semver,pattern={{major}}
#             type=raw,value=latest,enable={{is_default_branch}}
#             type=sha,prefix={{branch}}-

#       - name: Build and push SmartXDR Nginx image
#         uses: docker/build-push-action@v5
#         with:
#           context: ./nginx
#           file: ./nginx/Dockerfile
#           push: ${{ github.event_name != 'pull_request' }}
#           tags: ${{ steps.meta-nginx.outputs.tags }}
#           labels: ${{ steps.meta-nginx.outputs.labels }}
#           cache-from: type=gha
#           cache-to: type=gha,mode=max
#           platforms: linux/amd64
#           build-args: |
#             BUILDKIT_INLINE_CACHE=1

#       - name: Nginx image digest
#         run: echo ${{ steps.build-and-push-nginx.outputs.digest }}

#   security-scan:
#     runs-on: ubuntu-latest
#     needs: [build-and-push-core, build-and-push-nginx]
#     if: github.event_name != 'pull_request'
#     strategy:
#       matrix:
#         image: 
#           - wanthinnn/smartxdr-core
#           - wanthinnn/smartxdr-nginx
    
#     steps:
#       - name: Run Trivy vulnerability scanner
#         uses: aquasecurity/trivy-action@master
#         with:
#           image-ref: ${{ env.REGISTRY }}/${{ matrix.image }}:latest
#           format: 'sarif'
#           output: 'trivy-results-${{ matrix.image }}.sarif'

#       - name: Upload Trivy results to GitHub Security
#         uses: github/codeql-action/upload-sarif@v3
#         if: always()
#         with:
#           sarif_file: 'trivy-results-${{ matrix.image }}.sarif'
